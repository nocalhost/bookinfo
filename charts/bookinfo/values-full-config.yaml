# this is a normal case of nocalhost config
# use App Config as configmap as nocalhost install config
dep:
  enable: true
nocalhost:
  config:
    path: example/config-from-cm/nocalhost-full-config.yaml

  annotations:
    path:
      authors: ""
      details: ""
      productpage: ""
      ratings: ""
      reviews: ""