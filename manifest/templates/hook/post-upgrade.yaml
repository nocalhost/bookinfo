apiVersion: batch/v1
kind: Job
metadata:
  name: post-upgrade
spec:
  template:
    spec:
      containers:
      - name: post-upgrade
        image: codingcorp-docker.pkg.coding.net/nocalhost/public/alpine:latest
        command: ["sleep", "2"]
      restartPolicy: Never
  backoffLimit: 4