apiVersion: batch/v1
kind: Job
metadata:
  name: pre-delete
spec:
  template:
    spec:
      containers:
      - name: pre-delete
        image: codingcorp-docker.pkg.coding.net/nocalhost/public/alpine:latest
        command: ["sleep", "2"]
      restartPolicy: Never
  backoffLimit: 4